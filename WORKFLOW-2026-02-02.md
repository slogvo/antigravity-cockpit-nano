# WORKFLOW 2026-02-02: Implement i18n System

> **Objective**: Enable multi-language support (English & Vietnamese) for Antigravity Cockpit Nano.

## 📅 Timeline

- **Start Date**: 2026-02-02
- **Status**: 🚧 In Progress

## 📝 Todo List

### 1. Analysis & Preparation

- [ ] List all hardcoded strings in `package.json` (Commands, Configs).
- [ ] List all hardcoded strings in `src/**/*.ts` (UI, Messages).

### 2. Static Contributions (`package.json`)

VS Code extensions use `package.nls.json` for static text in `package.json`.

- [ ] Create `package.nls.json` (English - Default).
- [ ] Create `package.nls.vi.json` (Vietnamese).
- [ ] Create `package.nls.ja.json` (Japanese).
- [ ] Create `package.nls.ko.json` (Korean).
- [ ] Refactor `package.json`: Replace strings with `%key%` syntax.
    - [ ] `displayName`, `description`
    - [ ] `commands` titles
    - [ ] `configuration` descriptions & enums

### 3. Dynamic Source Code (`src/`)

VS Code extensions use `vscode.l10n` API for code strings.

- [ ] Create `l10n` folder root.
- [ ] Create `l10n/bundle.l10n.json` (English).
- [ ] Create `l10n/bundle.l10n.vi.json` (Vietnamese).
- [ ] Create `l10n/bundle.l10n.ja.json` (Japanese).
- [ ] Create `l10n/bundle.l10n.ko.json` (Korean).
- [ ] Refactor Code:
    - [ ] Import `l10n` from `vscode`.
    - [ ] Replace string literals with `l10n.t('key')` or `l10n.t('Access {0}', accessLevel)`.
    - **Target Files**:
        - `src/extension.ts` (Command registration messages)
        - `src/nano/nano_panel.ts` (Dashboard HTML context, Status bar tooltips)

### 4. Verification

- [ ] Run Extension in English (Default).
- [ ] Configure VS Code to use Vietnamese (`Configure Display Language`).
- [ ] Verify Dashboard UI.
- [ ] Verify Status Bar.
- [ ] Verify Settings/Configuration Panel.

## 🔍 Notes

- **API**: Use strictly standard `vscode.l10n` API (No external heavier libraries needed).
- **Structure**:
    ```
    root/
    ├── package.nls.json
    ├── package.nls.vi.json
    ├── l10n/
    │   ├── bundle.l10n.json
    │   └── bundle.l10n.vi.json
    └── ...
    ```
